// Importing the functions from their respective files
import { createVeloState } from "./store";
import { useVeloState } from "./useStore";

// Exporting them from the index file
export { createVeloState, useVeloState };
